<div class="conteiner">
  <h1>TOP投稿一覧（仮）</h1>
  <div class="row">
    <div class="col-3">
      <h3>登録が多いタグ</h3>
      <% if @tags.present? %>
        <% @tags.each do |tag| %>
          <%= link_to "#{tag.name}(#{tag.taggings_count})", tags_path(tag: tag.name) %>
        <% end %>
      <% else %>
        <p>登録されているタグはありません</p>
      <% end %>
    </div>
    <div class="col-9">
    <%= render 'index', posts: @posts %>
    </div>
  </div>
</div>