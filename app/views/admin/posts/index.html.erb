<div class="container">
  <h3>
		<% if params[:user_id] %>
			<%= @user.name %>さんの投稿履歴一覧
		<% else %>
			<h3>Posts</h3>
		<% end %>
	</h3>
  <div class="row">
    <%= render 'index', posts: @posts %>
  </div>
</div>